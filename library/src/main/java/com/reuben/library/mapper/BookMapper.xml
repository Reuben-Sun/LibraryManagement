<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.reuben.library.dao.BookDao">
    <select id="getBookList" resultType="com.reuben.library.pojo.Book">
        select * from books
    </select>
    <select id="getBookById" resultType="com.reuben.library.pojo.Book">
        select * from books where id=#{id}
    </select>
    <select id="getBookList2" resultType="map">
        select * from books where id=1
    </select>
    <select id="getPersonList" resultType="map">
        select * from person where id=1
    </select>
    <select id="getBookNameList" resultMap="bookNameMap">
        select id,name
        from books

    </select>

    <resultMap id="bookNameMap" type="java.util.Map">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
    </resultMap>
</mapper>